# Android Release Signing Configuration
# 
# SECURITY: This file contains sensitive signing credentials.
# 
# INSTRUCTIONS:
# 1. Copy this file to keystore.properties (without .example)
# 2. Replace all placeholder values with your actual signing credentials
# 3. Add keystore.properties to .gitignore (DO NOT commit this file!)
# 4. Store your keystore file securely and back it up
#
# To generate a new keystore:
#   keytool -genkey -v -keystore ~/ozpos-release-key.jks -keyalg RSA -keysize 2048 -validity 10000 -alias ozpos
#
# For more information, see:
# https://developer.android.com/studio/publish/app-signing

# Key alias name (the name you used when creating the keystore)
keyAlias=ozpos

# Key password (the password for the key alias)
keyPassword=YOUR_KEY_PASSWORD_HERE

# Path to your keystore file (relative to android/ directory or absolute path)
# Example: ../ozpos-release-key.jks (if keystore is in project root)
# Example: /path/to/ozpos-release-key.jks (absolute path)
storeFile=../ozpos-release-key.jks

# Keystore password (the password for the keystore file)
storePassword=YOUR_KEYSTORE_PASSWORD_HERE

